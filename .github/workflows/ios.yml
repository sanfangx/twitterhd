name: iOS Build & Package (.ipa)

on:
  push:
    branches: [ "main", "master", "feature/**" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build iOS App (.ipa)
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode Version
        run: |
          sudo xcode-select -s /Applications/Xcode_15.4.app || sudo xcode-select -s /Applications/Xcode.app
          xcodebuild -version

      - name: Build iOS App (Unsigned Release)
        run: |
          set -o pipefail
          xcodebuild clean build \
            -project TweetImageDownloader.xcodeproj \
            -scheme TweetImageDownloader \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CONFIGURATION_BUILD_DIR=build/Release-iphoneos | tee build.log || (echo "=== COMPILER ERRORS ===" && grep -E "error:|warning:" build.log && exit 1)

      - name: Upload Build Log on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: Build-Failure-Log
          path: build.log

      - name: Package .ipa File (Payload)
        run: |
          mkdir -p Payload
          cp -R build/Release-iphoneos/TweetImageDownloader.app Payload/
          zip -qr TweetImageDownloader.ipa Payload

      - name: Upload .ipa Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TweetImageDownloader-iOS-IPA
          path: TweetImageDownloader.ipa
          retention-days: 30
